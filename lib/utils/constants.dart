// Import the flutter package
import 'package:flutter/material.dart';

// Define some constants for colors using const keyword and Color class
const kPrimaryColor = Color(0xFF3F51B5);
const kSecondaryColor = Color(0xFF9E9E9E);
const kBackgroundColor = Color(0xFFF5F5F5);

// Define some constants for fonts using const keyword and TextStyle class
const kTitleTextStyle = TextStyle(fontSize: 24.0, fontWeight: FontWeight.bold);
const kSubtitleTextStyle = TextStyle(fontSize: 18.0);
const kBodyTextStyle = TextStyle(fontSize: 16.0);

// Define some constants for sizes using const keyword and double type
const kDefaultPadding = 16.0;
const kDefaultMargin = 8.0;
const kDefaultBorderRadius = 8.0;

// Define some constants for urls using const keyword and String type
const kFirebaseUrl = 'https://your-app.firebaseio.com';
const kGoogleSignInUrl = 'https://accounts.google.com/o/oauth2/v2/auth';
